<h1>hello</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th>status</th>
    <th>published_at</th>
    <th colspan="3"></th>
  </tr>

<% @articles.each do |article| %>
  <tr>
    <td><%= article.title %></td>
    <td><%= article.body %></td>
    <td><%= article.published %></td>
    <td><%= article.published_at %></td>
    <td><%= article.comments.map{|comment| comment.body}%></td>
  </tr>
<% end %>
</table>


<%= @autor.articles.count%>
<%= @autor.articles.map {|article| article.comments.count}%>
<%= @autor.articles.map {|article| article.category.name}%>
<%= "salary"%>
<%= ((@autor.articles.map{|art| art.comments.count}.sum)*0.8)+((@autor.articles.count)*10)+10 %>


<%= link_to 'Back', articles_path %>
